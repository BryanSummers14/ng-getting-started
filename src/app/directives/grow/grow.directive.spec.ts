import { GrowDirective } from './grow.directive';

describe('GrowDirective', () => {
  it('should create an instance', () => {
    const directive = new GrowDirective();
    expect(directive).toBeTruthy();
  });
});
