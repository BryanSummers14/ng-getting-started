import { RoleToggleDirective } from './role-toggle.directive';

describe('RoleToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleToggleDirective();
    expect(directive).toBeTruthy();
  });
});
